# Optional. The app does NOT read this at build time — it prompts for the key
# in /settings (in-memory only by default, optionally sessionStorage for the tab).
# This file is here for documentation purposes only.
# VITE_ANTHROPIC_API_KEY=
