# No environment variables are required to build or run this app.
# Optional AI explanations use an Anthropic API key that you paste into the
# in-app dialog at runtime. The key is held in memory only — never stored on
# disk, never written to .env, localStorage, or sessionStorage.
